# Broker integration adapters — one module per broker (Milestone 1+)
# Phase 1 pilot: Upstox (ADR-012). AA evaluated in parallel (ADR-010).
#
# base.py       — shared BrokerAdapter interface (Milestone 2)
# upstox.py     — pilot broker adapter (Milestone 2)
# registry.py   — get_adapter(broker_name) lookup (Milestone 2)
