{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "web",
      "runtimeExecutable": "npm",
      "runtimeArgs": ["run", "dev", "--prefix", "apps/web"],
      "port": 3000
    },
    {
      "name": "api",
      "runtimeExecutable": "apps/api/venv/Scripts/python.exe",
      "runtimeArgs": [
        "-c",
        "import os; os.chdir('apps/api'); from app import create_app; create_app().run(host='127.0.0.1', port=5000, debug=True, use_reloader=False)"
      ],
      "port": 5000
    }
  ]
}
